# GitHub token for bundling skills (avoids rate limits)
# Create a token at: https://github.com/settings/tokens
# Only needs public repo read access
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
